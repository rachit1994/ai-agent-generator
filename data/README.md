# Folder: `data`

## Why this folder exists
This folder groups code and artifacts for `data` within the repository architecture.

## What is present
- `company-os-progress-checklist.md` (file): Implements a concrete part of this folder responsibility.
- `company-os-progress-rules.json` (file): Implements a concrete part of this folder responsibility.
- `medium-hard-sde-suite.jsonl` (file): Implements a concrete part of this folder responsibility.
- `sde-project-plan.example.json` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
