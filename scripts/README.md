# Folder: `scripts`

## Why this folder exists
This folder groups code and artifacts for `scripts` within the repository architecture.

## What is present
- `bump_version.py` (file): Implements a concrete part of this folder responsibility.
- `init-git-hooks.sh` (file): Implements a concrete part of this folder responsibility.
- `run-stage1-suite.sh` (file): Implements a concrete part of this folder responsibility.
- `stage1-cold-start-demo.sh` (file): Implements a concrete part of this folder responsibility.
- `version-index-only.sh` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
