# `src/runtime`

- Purpose: Runtime behavior contracts and execution semantics definitions.
- Scope: Folder path is `src/runtime`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `runtime`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
