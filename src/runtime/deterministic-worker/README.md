# `src/runtime/deterministic-worker`

- Purpose: Deterministic worker runtime implementation and support components.
- Scope: Folder path is `src/runtime/deterministic-worker`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `runtime/deterministic-worker`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
