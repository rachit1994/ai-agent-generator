# Folder: `src/scalability_strategy/full_build_order_progression`

## Why this folder exists
This folder provides deterministic local full-build-order progression artifact generation and validation.

## What is present
- `__init__.py` (file): Public exports for progression runtime and contracts.
- `contracts.py` (file): Contract constants and fail-closed validators.
- `runtime.py` (file): Deterministic progression derivation.
- `surface.py` (file): Feature-surface metadata.
- `tests/` (folder): Focused runtime and contract tests.

