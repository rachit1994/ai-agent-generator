# `src/services/orchestrator/api`

- Purpose: API surface definitions, handlers, and API-level wiring.
- Scope: Folder path is `src/services/orchestrator/api`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `services/orchestrator/api`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
