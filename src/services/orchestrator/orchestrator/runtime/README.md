# `orchestrator.runtime`

Implementation package: execution modes, model adapter, storage, CTO gate helpers, and CLI wiring.

Public entrypoints for other code live in **`orchestrator.api`**; import from there unless you are extending runtime internals.
