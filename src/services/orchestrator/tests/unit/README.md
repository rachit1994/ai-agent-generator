# `src/services/orchestrator/tests/unit`

- Purpose: Unit test cases and test utilities scoped to a single module behavior.
- Scope: Folder path is `src/services/orchestrator/tests/unit`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `services/orchestrator/tests/unit`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
