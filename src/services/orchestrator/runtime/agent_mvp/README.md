# `src/services/orchestrator/runtime/agent_mvp`

- Purpose: MVP agent runtime package for orchestration execution and evaluation.
- Scope: Folder path is `src/services/orchestrator/runtime/agent_mvp`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `services/orchestrator/runtime/agent_mvp`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
