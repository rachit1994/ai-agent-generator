# `src/tests`

- Purpose: Test code validating behavior and contracts for src components.
- Scope: Folder path is `src/tests`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `tests`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
