# `src/infra/backup-restore`

- Purpose: Backup and restore primitives, scripts, or adapters for system state.
- Scope: Folder path is `src/infra/backup-restore`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `infra/backup-restore`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
