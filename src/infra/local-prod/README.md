# `src/infra/local-prod`

- Purpose: Local production-like environment profiles and support artifacts.
- Scope: Folder path is `src/infra/local-prod`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `infra/local-prod`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
