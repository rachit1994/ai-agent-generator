# `src/infra/monitoring`

- Purpose: Monitoring integrations, metrics wiring, and observability helpers.
- Scope: Folder path is `src/infra/monitoring`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `infra/monitoring`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
