# `src/tools/schema-upcaster`

- Purpose: Schema upcaster tooling for safe compatibility transformations.
- Scope: Folder path is `src/tools/schema-upcaster`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `tools/schema-upcaster`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
