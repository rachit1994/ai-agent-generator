# `src/tools/contract-lint`

- Purpose: Contract linting tooling that validates schema and policy consistency.
- Scope: Folder path is `src/tools/contract-lint`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `tools/contract-lint`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
