# `src/tools/incident-forensics`

- Purpose: Incident forensics utilities for investigation and evidence collection.
- Scope: Folder path is `src/tools/incident-forensics`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `tools/incident-forensics`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
