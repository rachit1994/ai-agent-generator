# `src/tools`

- Purpose: Developer/ops tooling code for validation, diagnostics, and maintenance workflows.
- Scope: Folder path is `src/tools`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `tools`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
