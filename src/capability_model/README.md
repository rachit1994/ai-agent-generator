# Folder: `src/capability_model`

## Why this folder exists
This folder groups code and artifacts for `capability_model` within the repository architecture.

## What is present
- `capability_graph` (folder): Groups related implementation for this subdomain boundary.
- `dependency_aware_promotion_eligibility` (folder): Groups related implementation for this subdomain boundary.
- `scoring_confidence_decay` (folder): Groups related implementation for this subdomain boundary.
- `transfer_learning_measurement` (folder): Groups related implementation for this subdomain boundary.
- `__init__.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
