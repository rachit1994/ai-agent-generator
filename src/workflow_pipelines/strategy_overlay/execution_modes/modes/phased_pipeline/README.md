# Folder: `src/workflow_pipelines/strategy_overlay/execution_modes/modes/phased_pipeline`

## Why this folder exists
This folder groups code and artifacts for `phased_pipeline` within the repository architecture.

## What is present
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `decompose.py` (file): Implements a concrete part of this folder responsibility.
- `pipeline.py` (file): Implements stage/pipeline behavior for task execution lifecycle.
- `prompts.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
