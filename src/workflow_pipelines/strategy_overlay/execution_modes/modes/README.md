# Folder: `src/workflow_pipelines/strategy_overlay/execution_modes/modes`

## Why this folder exists
This folder groups code and artifacts for `modes` within the repository architecture.

## What is present
- `baseline` (folder): Groups related implementation for this subdomain boundary.
- `guarded_pipeline` (folder): Implements stage/pipeline behavior for task execution lifecycle.
- `phased_pipeline` (folder): Implements stage/pipeline behavior for task execution lifecycle.
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `guarded.py` (file): Implements a concrete part of this folder responsibility.
- `phased.py` (file): Implements a concrete part of this folder responsibility.
- `README.md` (file): Documents folder behavior and usage context.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
