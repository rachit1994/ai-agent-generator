# `src/libraries`

- Purpose: Shared libraries and reusable internal modules used across src.
- Scope: Folder path is `src/libraries`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `libraries`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
