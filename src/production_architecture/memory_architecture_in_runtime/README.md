# Folder: `src/production_architecture/memory_architecture_in_runtime`

## Why this folder exists
This folder provides deterministic local runtime memory-architecture artifact generation and validation.

## What is present
- `__init__.py` (file): Public exports for runtime and contracts.
- `contracts.py` (file): Contract constants and fail-closed validators.
- `runtime.py` (file): Deterministic memory-architecture derivation.
- `surface.py` (file): Feature-surface metadata.
- `tests/` (folder): Focused runtime and contract tests.
