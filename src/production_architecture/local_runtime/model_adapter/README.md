# Folder: `src/production_architecture/local_runtime/model_adapter`

## Why this folder exists
This folder groups code and artifacts for `model_adapter` within the repository architecture.

## What is present
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `model_adapter.py` (file): Wraps model-facing logic and invocation behavior.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
