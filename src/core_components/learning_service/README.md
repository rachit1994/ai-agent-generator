# Folder: `src/core_components/learning_service`

## Why this folder exists
This folder groups code and artifacts for `learning_service` within the repository architecture.

## What is present
- `contracts.py`: Contract/validation for `learning/learning_service.json`.
- `runtime.py`: Deterministic learning-service runtime derivation.
- `surface.py`: Implemented surface metadata for orchestrator discovery.
- `tests/test_runtime.py`: Determinism and fail-closed validation tests.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
