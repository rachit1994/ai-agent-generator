# Folder: `src/core_components/memory_system`

## Why this folder exists
This folder groups code and artifacts for `memory_system` within the repository architecture.

## What is present
- `contracts.py`: Contract/validation for `memory/memory_system.json`.
- `runtime.py`: Deterministic memory-system runtime derivation.
- `surface.py`: Implemented surface metadata for orchestrator discovery.
- `tests/test_runtime.py`: Determinism and fail-closed validation tests.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
