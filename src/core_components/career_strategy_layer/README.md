# Folder: `src/core_components/career_strategy_layer`

## Why this folder exists
This folder groups code and artifacts for `career_strategy_layer` within the repository architecture.

## What is present
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `surface.py` (file): Implements a concrete part of this folder responsibility.
- `contracts.py` (file): Career strategy layer artifact contract and validator.
- `runtime.py` (file): Deterministic career strategy layer derivation.
- `tests/` (folder): Unit tests for career strategy layer runtime.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
