# Folder: `src/evaluation_framework/promotion_evaluation`

## Why this folder exists
This folder groups code and artifacts for `promotion_evaluation` within the repository architecture.

## What is present
- `contracts.py`: Contract/validation for `learning/promotion_evaluation.json`.
- `runtime.py`: Deterministic promotion-evaluation derivation.
- `surface.py`: Implemented surface metadata for orchestrator discovery.
- `tests/test_runtime.py`: Determinism and fail-closed validation tests.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
