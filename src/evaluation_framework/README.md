# Folder: `src/evaluation_framework`

## Why this folder exists
This folder groups code and artifacts for `evaluation_framework` within the repository architecture.

## What is present
- `offline_evaluation` (folder): Groups related implementation for this subdomain boundary.
- `online_evaluation` (folder): Groups related implementation for this subdomain boundary.
- `promotion_evaluation` (folder): Groups related implementation for this subdomain boundary.
- `regression_testing_surface` (folder): Provides validation coverage for this folder behavior.
- `__init__.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
