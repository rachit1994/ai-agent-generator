# Folder: `src/evaluation_framework/offline_evaluation`

## Why this folder exists
This folder groups code and artifacts for `offline_evaluation` within the repository architecture.

## What is present
- `sde_eval` (folder): Groups related implementation for this subdomain boundary.
- `__init__.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
