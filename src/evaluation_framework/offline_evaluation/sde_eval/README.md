# Folder: `src/evaluation_framework/offline_evaluation/sde_eval`

## Why this folder exists
This folder groups code and artifacts for `sde_eval` within the repository architecture.

## What is present
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `eval.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
