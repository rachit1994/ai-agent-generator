# Folder: `src/success_criteria`

## Why this folder exists
This folder groups code and artifacts for `success_criteria` within the repository architecture.

## What is present
- `capability_growth_metrics` (folder): Groups related implementation for this subdomain boundary.
- `error_reduction_metrics` (folder): Groups related implementation for this subdomain boundary.
- `extended_binary_gates` (folder): Implements gate logic used to approve/reject progression.
- `hard_release_gates` (folder): Implements gate logic used to approve/reject progression.
- `stability_metrics` (folder): Groups related implementation for this subdomain boundary.
- `transfer_learning_metrics` (folder): Groups related implementation for this subdomain boundary.
- `__init__.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
