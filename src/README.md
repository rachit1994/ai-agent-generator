# Folder: `src`

## Why this folder exists
This folder groups code and artifacts for `src` within the repository architecture.

## What is present
- `agent_lifecycle` (folder): Groups related implementation for this subdomain boundary.
- `agent_organization` (folder): Groups related implementation for this subdomain boundary.
- `capability_model` (folder): Wraps model-facing logic and invocation behavior.
- `core_components` (folder): Groups related implementation for this subdomain boundary.
- `evaluation_framework` (folder): Groups related implementation for this subdomain boundary.
- `event_sourced_architecture` (folder): Handles event lineage, event contracts, or event persistence logic.
- `guardrails_and_safety` (folder): Groups related implementation for this subdomain boundary.
- `human_professional_evolution_model` (folder): Wraps model-facing logic and invocation behavior.
- `learning_evolution_engine` (folder): Groups related implementation for this subdomain boundary.
- `memory_architecture` (folder): Implements memory handling and memory-related lifecycle logic.
- `production_architecture` (folder): Groups related implementation for this subdomain boundary.
- `scale_out_api_mcp_plugin_platform_features` (folder): Isolated per-feature implementation surfaces for API/MCP scale-out architecture work.
- `scalability_strategy` (folder): Groups related implementation for this subdomain boundary.
- `service_boundaries` (folder): Groups related implementation for this subdomain boundary.
- `success_criteria` (folder): Groups related implementation for this subdomain boundary.
- `workflow_pipelines` (folder): Implements stage/pipeline behavior for task execution lifecycle.
- `.gitkeep` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
