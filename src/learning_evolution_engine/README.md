# Folder: `src/learning_evolution_engine`

## Why this folder exists
This folder groups code and artifacts for `learning_evolution_engine` within the repository architecture.

## What is present
- `capability_improvement_over_windows` (folder): Groups related implementation for this subdomain boundary.
- `causal_closure_contract` (folder): Defines machine-checkable interface and schema expectations.
- `learning_updates` (folder): Groups related implementation for this subdomain boundary.
- `practice_loop` (folder): Groups related implementation for this subdomain boundary.
- `reflection_loop_chain` (folder): Groups related implementation for this subdomain boundary.
- `skill_gap_detection` (folder): Groups related implementation for this subdomain boundary.
- `__init__.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
