# `src/contracts/policy`

- Purpose: Policy contracts and policy evaluation schema definitions.
- Scope: Folder path is `src/contracts/policy`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `contracts/policy`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
