# `src/contracts/service`

- Purpose: Service contracts for service-level APIs, responsibilities, and expectations.
- Scope: Folder path is `src/contracts/service`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `contracts/service`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
