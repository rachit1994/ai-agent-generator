# `src/contracts`

- Purpose: Source-of-truth contracts describing interfaces across system boundaries.
- Scope: Folder path is `src/contracts`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `contracts`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
