# `src/contracts/event`

- Purpose: Event contracts for emitted/consumed event shapes and semantics.
- Scope: Folder path is `src/contracts/event`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `contracts/event`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
