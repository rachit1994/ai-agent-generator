# `src/contracts/capability`

- Purpose: Capability contract definitions and capability-level invariants.
- Scope: Folder path is `src/contracts/capability`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `contracts/capability`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
