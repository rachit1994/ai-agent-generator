# `src/contracts/authz`

- Purpose: Authorization contracts defining permission models and policy decisions.
- Scope: Folder path is `src/contracts/authz`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `contracts/authz`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
