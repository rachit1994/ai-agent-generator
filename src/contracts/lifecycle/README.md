# `src/contracts/lifecycle`

- Purpose: Lifecycle contracts for startup, execution, teardown, and transitions.
- Scope: Folder path is `src/contracts/lifecycle`.
- Should contain: Should contain implementation files, schemas, tests, or docs directly relevant to `contracts/lifecycle`.
- Should not contain: Should not contain unrelated domain logic or artifacts owned by sibling folders under `src/`.
- Keep files here focused, small, and aligned with this folder contract.
