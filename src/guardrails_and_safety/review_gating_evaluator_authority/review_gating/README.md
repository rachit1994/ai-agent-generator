# Folder: `src/guardrails_and_safety/review_gating_evaluator_authority/review_gating`

## Why this folder exists
This folder groups code and artifacts for `review_gating` within the repository architecture.

## What is present
- `tests` (folder): Provides validation coverage for this folder behavior.
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `policy_bundle_rollback.py` (file): Implements a concrete part of this folder responsibility.
- `review.py` (file): Implements review and gating logic for quality/safety decisions.
- `run_directory.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
