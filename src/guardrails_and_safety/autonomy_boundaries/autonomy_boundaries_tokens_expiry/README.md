# Folder: `src/guardrails_and_safety/autonomy_boundaries/autonomy_boundaries_tokens_expiry`

## Why this folder exists
This folder groups code and artifacts for `autonomy_boundaries_tokens_expiry` within the repository architecture.

## What is present
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `token_context.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
