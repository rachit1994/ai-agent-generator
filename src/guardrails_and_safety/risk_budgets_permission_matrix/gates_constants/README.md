# Folder: `src/guardrails_and_safety/risk_budgets_permission_matrix/gates_constants`

## Why this folder exists
This folder groups code and artifacts for `gates_constants` within the repository architecture.

## What is present
- `__init__.py` (file): Implements a concrete part of this folder responsibility.
- `constants.py` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
