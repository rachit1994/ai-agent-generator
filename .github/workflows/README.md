# Folder: `.github/workflows`

## Why this folder exists
This folder groups code and artifacts for `workflows` within the repository architecture.

## What is present
- `ci.yml` (file): Implements a concrete part of this folder responsibility.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
