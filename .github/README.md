# Folder: `.github`

## Why this folder exists
This folder groups code and artifacts for `.github` within the repository architecture.

## What is present
- `workflows` (folder): Groups related implementation for this subdomain boundary.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
