# Folder: `tests`

## Why this folder exists
This folder groups code and artifacts for `tests` within the repository architecture.

## What is present
- `test_version_bump.py` (file): Provides validation coverage for this folder behavior.

## Notes
- Keep this inventory updated when adding/removing files.
- Prefer placing tests close to the most specific leaf module.
